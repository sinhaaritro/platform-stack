apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: readarr-ingressroute
  namespace: readarr
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`readarr.localhost`)
      kind: Rule
      services:
        - name: readarr-service
          port: 8787
