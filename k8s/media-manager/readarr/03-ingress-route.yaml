apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: readarr-ingressroute
  namespace: media-manager
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`readarr.localdev`)
      kind: Rule
      services:
        - name: readarr-service
          port: 8787
