apiVersion: v1
kind: Service
metadata:
  name: authentik-outpost-service
  namespace: auth
spec:
  selector:
    app: authentik-outpost
  ports:
    - name: http
      protocol: TCP
      port: 9000
      targetPort: 9000
