apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik-ingressroute
  namespace: auth
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`auth.localdev`)
      kind: Rule
      services:
        - name: authentik-server-service
          port: 9000
