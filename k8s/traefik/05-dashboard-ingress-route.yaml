apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
    # This rule redirects http://traefik.localhost to http://traefik.localhost/dashboard/
    - match: Host(`traefik.localhost`) && PathPrefix(`/`)
      kind: Rule
      priority: 100
      services:
        - name: api@internal
          kind: TraefikService
      middlewares:
        - name: traefik-dashboard-redirect
    # This rule handles the dashboard itself
    - match: Host(`traefik.localhost`) && PathPrefix(`/dashboard`)
      kind: Rule
      priority: 101
      services:
        - name: api@internal
          kind: TraefikService
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefik-dashboard-redirect
  namespace: traefik
spec:
  redirectRegex:
    regex: ^https?://([^/]+)/?$
    replacement: http://${1}/dashboard/
    permanent: true
