apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: authentik
spec:
  storageClassName: standard # Using the default 'local-path' provisioner
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi # 8GB is a good starting size for the database
