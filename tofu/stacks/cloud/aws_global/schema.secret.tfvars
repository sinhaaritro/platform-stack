# -----------------------------------------------------------------------------
# SECRETS - AWS GLOBAL RESOURCES
# -----------------------------------------------------------------------------
# This file must be encrypted with Ansible Vault before running OpenTofu.
# To encrypt it, run:
#   ansible-vault encrypt tofu/stacks/cloud/aws_global/schema.secret.tfvars
# -----------------------------------------------------------------------------

# Add any sensitive credentials here if needed (e.g., custom access keys).
# If none are required because you are using environment variables, you can
# keep this file empty but vault-encrypted.
