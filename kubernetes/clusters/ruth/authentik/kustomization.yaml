apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: security

resources:
  - ../../../apps/infrastructure/authentik/base

components:
  - ../../../apps/infrastructure/authentik/components/security-context
  - ../../../apps/infrastructure/authentik/components/secrets
  # - ../../../apps/infrastructure/authentik/components/redis # Old component (volume patch) likely obsolete if chart is new
  # - ../../../apps/infrastructure/authentik/components/redis-standalone # Moved to standalone app
  - ../../../apps/infrastructure/authentik/components/postgresql
  - ../../../apps/infrastructure/authentik/components/ingress
