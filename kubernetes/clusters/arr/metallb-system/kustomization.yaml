apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/infrastructure/metallb/base

components:
  - ../../../apps/infrastructure/metallb/components/minimal
  - ../../../apps/infrastructure/metallb/components/resource-limits
patches:
  - path: pool-patch.yaml
    target:
      kind: IPAddressPool
      name: default-pool
