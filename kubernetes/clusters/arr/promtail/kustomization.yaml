apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../apps/infrastructure/promtail/overlays/minimal

patches:
  - path: config-patch.yaml
    target:
      kind: HelmRelease
      name: promtail
