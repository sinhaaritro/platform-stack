apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-cert-strawslabs
  namespace: personal
  labels:
    backuplabel.certificate: "true"
spec:
  secretName: immich-cert-strawslabs-tls
  issuerRef:
    name: letsencrypt-prod-strawslabs
    kind: ClusterIssuer
  dnsNames:
    - "immich-hyperion.strawslabs.com"
  secretTemplate:
    labels:
      backuplabel.certificate: "true"