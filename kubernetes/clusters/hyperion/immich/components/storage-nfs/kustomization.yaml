apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - target:
      kind: PersistentVolumeClaim
      name: immich-library
    patch: |-
      - op: replace
        path: /spec/storageClassName
        value: nfs