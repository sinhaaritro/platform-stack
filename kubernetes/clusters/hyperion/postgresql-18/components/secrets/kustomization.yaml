apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - sealed-postgres-18-admin.yaml

patches:
  - path: patch-env.yaml
    target:
      kind: StatefulSet
      name: postgresql-18
