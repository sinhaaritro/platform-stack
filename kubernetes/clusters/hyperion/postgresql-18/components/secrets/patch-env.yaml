apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgresql-18
spec:
  template:
    spec:
      containers:
        - name: postgres
          envFrom:
            - secretRef:
                name: postgres-18-admin
