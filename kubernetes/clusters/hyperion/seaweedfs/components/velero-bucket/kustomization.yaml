apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - sealed-velero-s3.yaml

patches:
  - path: patch-velero-job.yaml
    target:
      kind: Job
      name: s3-velero-provision
      namespace: storage
