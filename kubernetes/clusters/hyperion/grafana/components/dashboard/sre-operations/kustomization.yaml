apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  - sre-operations.yaml
  - dashboard-backup-dr.yaml

configMapGenerator:
  - name: grafana-dashboard-backup-dr
    namespace: monitoring
    files:
      - backup-dr.json
    options:
      disableNameSuffixHash: true
