apiVersion: velero.io/v1
kind: BackupStorageLocation
metadata:
  name: aws
  namespace: backup
spec:
  provider: aws
  objectStorage:
    bucket: hyperion-velero-backups
  config:
    region: ap-southeast-1
  credential:
    name: velero-aws-creds
    key: cloud
