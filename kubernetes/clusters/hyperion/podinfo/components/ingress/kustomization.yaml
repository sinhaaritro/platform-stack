apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: podinfo

patches:
  - patch: |
      - op: replace
        path: /spec/rules/0/host
        value: "podinfo.hyperion.example.com"
    target:
      kind: Ingress
      name: podinfo
