apiVersion: apps/v1
kind: Deployment
metadata:
  name: podinfo
  namespace: podinfo
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: podinfo
          args:
            - --level=$(PODINFO_LEVEL)
          envFrom:
            - configMapRef:
                name: podinfo-dev-debug
