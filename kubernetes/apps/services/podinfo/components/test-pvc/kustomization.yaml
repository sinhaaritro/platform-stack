apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Adds a test PVC to podinfo for Velero backup verification.
# This component is TEMPORARY — remove after backup tests pass.

resources:
  - pvc.yaml

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: podinfo
    path: patch-mount.yaml
