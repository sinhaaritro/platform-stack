apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich
  labels:
    app.kubernetes.io/instance: immich
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 50Gi
