apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - sealed-immich-db-credentials.yaml

patches:
  - path: patch-env.yaml
    target:
      kind: Deployment
      name: immich-server
