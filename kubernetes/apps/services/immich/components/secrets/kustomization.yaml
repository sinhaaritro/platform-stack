apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - sealed-immich-db-credentials.yaml
  - external-postgres-admin.yaml
  - cluster-secret-store.yaml

patches:
  - path: patch-env.yaml
    target:
      kind: Deployment
      name: immich-server
