apiVersion: batch/v1
kind: Job
metadata:
  name: immich-init-db
spec:
  template:
    spec:
      containers:
        - name: init-db
          env:
            - name: DB_PASSWORD
              value: "superadminpassword" # Override or set specific value
