apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
namespace: storage

configMapGenerator:
  - name: pg-ext-immich
    files:
      - install.sh
    options:
      disableNameSuffixHash: true

patches:
  - path: patch-statefulset.yaml