apiVersion: v1
kind: Secret
metadata:
  name: external-dns-secret
  namespace: external-dns
type: Opaque
stringData:
  # Placeholder - User needs to fill this or use robust secrets management
  credentials: |
    [default]
    aws_access_key_id = CHANGE_ME
    aws_secret_access_key = CHANGE_ME
