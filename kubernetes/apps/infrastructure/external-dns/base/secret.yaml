apiVersion: v1
kind: Secret
metadata:
  name: external-dns-secret
  namespace: external-dns
type: Opaque
stringData:
  # Placeholder - User needs to fill this with Cloudflare API Token
  api-token: CHANGE_ME
