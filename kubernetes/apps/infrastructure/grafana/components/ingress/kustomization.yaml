apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
metadata:
  name: ingress
# Template / dummy values. To expose grafana on a cluster, copy this folder to
# clusters/<name>/grafana/components/ingress and replace every "dummy" value.
resources:
  - ingress.yaml
  - cert-dummy.yaml