apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cert-retry-binding
subjects:
  - kind: ServiceAccount
    name: cert-retry-sa
    namespace: networking
roleRef:
  kind: ClusterRole
  name: cert-retry-role
  apiGroup: rbac.authorization.k8s.io
