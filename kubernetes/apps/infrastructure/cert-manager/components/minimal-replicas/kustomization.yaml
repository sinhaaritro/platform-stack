apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
  - target:
      kind: Deployment
      name: cert-manager.*
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
