architecture: standalone
auth:
  enabled: true
  # Password is rendered into the ConfigMap by Helm (Helm cannot read k8s secrets at render time).
  # Keep this in sync with redis-secret.password.
  password: "change-me-please-redis-password"
  existingSecret: redis-secret
  existingSecretPasswordKey: password

primary:
  persistence:
    enabled: false
