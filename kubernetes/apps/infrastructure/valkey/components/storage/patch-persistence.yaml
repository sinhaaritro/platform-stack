apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: redis-valkey
  namespace: storage
spec:
  template:
    spec:
      volumes:
        - name: data
          $patch: delete
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "longhorn"
        resources:
          requests:
            storage: 1Gi
