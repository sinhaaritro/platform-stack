apiVersion: v1
kind: Secret
metadata:
  name: {{ target_cluster_group }}-cluster-secret
  namespace: {{ argocd_namespace }}
  labels:
    argocd.argoproj.io/secret-type: cluster
type: Opaque
stringData:
  name: {{ target_cluster_group }}
  server: {{ argocd_remote_api_url }}
  config: |
    {
      "bearerToken": "{{ argocd_remote_token }}",
      "tlsClientConfig": {
        "insecure": false,
        "caData": "{{ argocd_remote_ca_b64 }}"
      }
    }
