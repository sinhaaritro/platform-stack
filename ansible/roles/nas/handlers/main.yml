---
# handlers/main.yml - Service restarts for the nas role

- name: Export NFS filesystems
  ansible.builtin.command: exportfs -ra
  changed_when: false
  listen: Export NFS filesystems

- name: Restart smbd
  ansible.builtin.service:
    name: smbd
    state: restarted
